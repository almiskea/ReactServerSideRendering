import React from 'react';

const Home = () => {
  return (
    <div>
      <div>I am the bast ever home inin component</div>
      <button onClick={() => console.log('Hi theere!')}>Press me!</button>
    </div>
  );
};

export default Home;
